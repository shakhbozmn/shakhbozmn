{
  "name": "shakhbozmn-profile-clock",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/clock.test.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy && node scripts/bump-cache-version.js",
    "bump": "node scripts/bump-cache-version.js"
  },
  "devDependencies": {
    "gifenc": "^1.0.3",
    "wrangler": "^4.114.0"
  }
}
